# =============================================================
# Environment variables for ciya-dev
# Copy to .env and edit:
#   cp .env.example .env
#   vi .env
# up.sh sources .env automatically on startup.
# =============================================================

# Anthropic authentication (choose one):
#   Subscription (OAuth):  Run `claude login` — no env var needed
#   API Key:               https://console.anthropic.com/settings/keys
#   Bedrock:               https://docs.aws.amazon.com/bedrock/
#   Vertex:                https://cloud.google.com/vertex-ai
# ANTHROPIC_API_KEY=sk-ant-xxxxx
# CLAUDE_CODE_USE_BEDROCK=1
# CLAUDE_CODE_USE_VERTEX=1

# GitHub CLI (gh) Fine-grained PAT
# Setup:
# 1. https://github.com/settings/personal-access-tokens/new
# 2. Repository access: Only select repositories → select this repo
# 3. Permissions:
#    - Contents: Read and write
#    - Pull requests: Read and write
#    - Issues: Read and write
#    - Actions: Read-only
#    - Commit statuses: Read-only
GH_TOKEN=github_pat_xxxxx

# Allowed domains file for sandbox hook
ALLOWED_DOMAINS_FILE=.claude/hooks/allowed-domains.txt
